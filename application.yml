management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
    refresh:
      enabled: true
  tracing:
    sampling:
      probability: 0.0

spring.zipkin.baseUrl: http://localhost:9411
